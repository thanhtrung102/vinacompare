apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: PostgreSQL
    type: postgres
    url: postgres:5432
    database: vinarag
    user: vinarag
    secureJsonData:
      password: vinarag123
    jsonData:
      sslmode: disable
    editable: true
